# /implement-feature

You are implementing a feature for the VNS travel booking platform (src/client = React frontend, src/server = Express backend, src/database = PostgreSQL schema).

Before writing any code:
1. Read @docs/booking-flows.md and @docs/business-rules.md
2. Identify which layers are affected (route / controller / service / repository / migration / frontend component)
3. List applicable edge cases and validation rules
4. Write a brief plan and wait for confirmation before coding

Feature to implement: $ARGUMENTS
